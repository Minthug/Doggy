[build]
builder = "nixpacks"

[build.nixpacksPlan.variables]
NIXPACKS_JDK_VERSION = "21"

[deploy]
startCommand = "java -Xmx400m -jar build/libs/backend-0.0.1-SNAPSHOT.jar"
healthcheckPath = "/actuator/health"
healthcheckTimeout = 300
